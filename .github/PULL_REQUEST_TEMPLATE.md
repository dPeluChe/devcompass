## Summary

<!-- 1–3 bullets describing what changed and why. -->

-

## Screenshots

<!-- For any UI change, drop a before / after. -->

## Test plan

- [ ] `npm run build` is green (0 errors, 0 warnings)
- [ ] Touched UI was opened in the browser and the golden path still works
- [ ] No new analytics, telemetry, third-party scripts, or backend dependencies
- [ ] Native `confirm()` / `alert()` / `prompt()` not introduced (use `<ConfirmDialog>`)
- [ ] GitHub-supplied HTML rendered through `<SanitizedMarkdown>` (DOMPurify)

## Related

<!-- Closes #N, refs #M, or "n/a". -->
